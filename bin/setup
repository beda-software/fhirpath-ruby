#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Vendor the ANTLR4 C++ runtime, generate the FHIRPath parser from its grammar (needs a JRE),
# and compile the native extension (needs a C++14 compiler). See rakelib/parser.rake.
bundle exec rake parser:setup compile
